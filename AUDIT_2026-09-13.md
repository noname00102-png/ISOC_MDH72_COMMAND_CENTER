# ISOC MDH72 Command Center — Audit 2026-09-13

Source: `ISOC_MDH72_COMMAND_CENTER_REPORT_DAILY_WEEKLY_SUPABASE_AUDITED_2026-09-12_FINAL_A4_EVENT_IMAGES_REPAIRED.html`

## Verified Supabase project
- Project ref: `riuebseoczwwifxezcwj`
- Status: ACTIVE_HEALTHY
- Region: ap-northeast-1
- Verified public tables: `profiles`, `threats`, `system_settings`, `audit_logs`, `threat_images`, `weather_forecast_history`
- Verified live row counts at audit time: threats 5, profiles 1, audit_logs 97, threat_images 14, weather_forecast_history 11.

## Real fixes applied to the uploaded HTML
1. Removed weather-history queries to non-existent `weather_forecast_weekly` and `weather_forecast_monthly` tables.
2. Weather history now reads only `weather_forecast_history`; weekly/monthly views are calculated from those verified daily rows.
3. Corrected the legacy threat writer to use the verified `public.threats` schema (`description`, `subdistrict`, etc.) instead of invalid `tambon`/`detail` columns.
4. Added a verified schema manifest to the runtime for diagnostics.
5. Re-ran JavaScript syntax validation for all 83 inline/external script blocks extracted from the HTML: PASS.

## Security/performance observations
- Supabase security advisor: leaked-password protection is disabled (WARN; account/auth setting, not changed automatically).
- Performance advisor reports 11 currently unused indexes (INFO); no indexes were removed because usage may change with production traffic.

## Scope note
Browser-only live authentication, Realtime subscription behavior, external GIS/tile CORS, and Storage signed-URL rendering cannot be truthfully certified by static parsing alone. The supplied HTML was therefore tested statically and the database schema was verified through the connected Supabase project rather than guessed.
